---
layout: base.njk
title: posts
eleventyImport:
    collections: ["posts"]
---

<h2>Posts</h2>
<ul>
    {% for post in (collections.posts | reverse) %}
        <li>
            {{ post.date | date }}
            -
            <a href="{{ post.url }}">{{ post.data.title }}</a>
        </li>
    {% endfor %}
</ul>